# GEMINI.md - TEDxBhrikutiMandap2026

This document provides instructional context for AI interactions within the `tedxbhrikutimandap2026` project.

## Project Overview

The **TEDxBhrikutiMandap2026** project is a web application developed for the TEDx event at Bhrikuti Mandap in 2026. It is built using the **Next.js** framework (App Router) and **React 19**.

### Key Technologies

*   **Framework:** [Next.js](https://nextjs.org/) (Version 16.1.6 - App Router)
*   **UI Library:** [React 19](https://react.dev/)
*   **Styling:** [Tailwind CSS 4](https://tailwindcss.com/)
*   **Language:** [TypeScript](https://www.typescriptlang.org/)
*   **Package Manager:** [pnpm](https://pnpm.io/)

### Architecture

The project follows the standard Next.js App Router architecture:
*   `app/`: Contains routes, layouts, and page components.
*   `public/`: Static assets (images, icons, etc.).
*   `next.config.ts`: Configuration for Next.js features and build options.
*   `tailwind.config.ts`: Tailwind CSS configuration (Note: Tailwind 4 uses `@import "tailwindcss"` in `globals.css` with inline theme configuration).

## Building and Running

The project uses `pnpm` as the primary package manager.

| Task | Command |
| :--- | :--- |
| **Development** | `pnpm dev` |
| **Build** | `pnpm build` |
| **Production Start** | `pnpm start` |
| **Linting** | `pnpm lint` |

### Environment Setup

*   Ensure you have [Node.js](https://nodejs.org/) (check `package.json` for engine requirements, defaults to Node 20+).
*   Install `pnpm` globally if not already available: `npm install -g pnpm`.
*   Install dependencies: `pnpm install`.

## Development Conventions

*   **App Router:** Always use the `app/` directory for new routes and layouts.
*   **Styling:** Use Tailwind CSS 4 utility classes. Theme overrides should be made in `app/globals.css` using the `@theme` directive.
*   **TypeScript:** Maintain strict type safety. Avoid using `any` and define interfaces/types for props and state.
*   **Components:** Prefer functional components with React Hooks. Use Server Components by default; add `"use client"` only when client-side interactivity or hooks (like `useState`, `useEffect`) are required.
*   **Linting:** Adhere to the rules defined in `eslint.config.mjs` (Core Web Vitals + TypeScript recommendations).
*   **Formatting:** Follow the project's formatting style (standard Prettier/ESLint defaults for Next.js).

---

*Note: This file is automatically generated and updated as the project evolves.*
